<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">

	<!-- 게시판 글 작성 -->
	<insert id="insert">
		INSERT INTO MP_BOARD(
		TITLE
		, CONTENT
		, WRITER )
		VALUES(
		#{title}
		, #{content}
		, #{writer} )
	</insert>

	<select id="listPage" resultType="com.rilla.vo.BoardVO"
		parameterType="com.rilla.vo.Criteria">
		SELECT BNO,
		TITLE,
		CONTENT,
		WRITER,
		REGDATE
		FROM (
		SELECT BNO,
		TITLE,
		CONTENT,
		WRITER,
		REGDATE,
		ROW_NUMBER() OVER(ORDER BY BNO ASC) AS
		RNUM
		FROM MP_BOARD
		) MP
		WHERE RNUM BETWEEN #{rowStart} AND #{rowEnd}
	</select>

	<select id="listCount" resultType="int">
	<![CDATA[
		SELECT COUNT(BNO)
		  FROM MP_BOARD
		 WHERE BNO > 0
	]]>
	</select>

	<select id="read" parameterType="int"
		resultType="com.rilla.vo.BoardVO">
		SELECT BNO
		, TITLE
		, CONTENT
		, WRITER
		, REGDATE
		FROM MP_BOARD
		WHERE BNO = #{bno}
	</select>

	<update id="update" parameterType="com.rilla.vo.BoardVO">
		UPDATE MP_BOARD
		SET TITLE =
		#{title},
		CONTENT = #{content}
		WHERE BNO = #{bno}
	</update>

	<delete id="delete" parameterType="int">
		DELETE
		FROM MP_BOARD
		WHERE BNO =
		#{bno}
	</delete>

</mapper>